# v0.2 fixes

- Removed unsupported `attributes:` from navbar items in `_quarto.yml`; language styling remains handled by the existing JavaScript helper.
- Wrapped all custom HTML in explicit raw HTML blocks so nested cards, logos and project hero sections render as HTML rather than code.
- Spanish version retained; English remains the main language.
