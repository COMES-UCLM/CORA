# Validation v0.2

- `_quarto.yml` parsed as valid YAML.
- Unsupported `navbar:right:attributes` removed.
- Every `.qmd` was passed through Pandoc 3.1.11.1 and checked for escaped custom HTML blocks.
- English remains the main language; Spanish version retained.

Final `quarto preview` / `quarto render` should be executed with the local Quarto installation.
